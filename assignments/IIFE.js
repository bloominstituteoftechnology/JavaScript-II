console.log((function() {
    var name = "Nutso";
    return name;
})());

// Immediately invoked function expression uses () after the definition to indicate that it is run as soon as it is defined. 